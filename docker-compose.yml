version: "3.8"

services:
  todo:
    container_name: todo-task
    image: sscream/item-todo:latest
    ports:
      - "7000:5000"
    volumes:
      - ./todos.json:/app/todos.json
    networks:
      - skola-net

networks:
  skola-net:
    name: skola-net
    driver: bridge
